from timeit import timeit

count = 1
format = "%16s%8.3f %s"

statement = "s = ''.join(['a' for n in range(" + str(count) + ")])"
print "comprehensions {:8.3f} {}".format(timeit(statement, number=1), statement)
