

from scipy.stats import norm
import numpy as np
from scipy.sparse.linalg.isolve.lgmres import norm2
print norm.cdf(1.96)
# 
# array(0.97500210485177952)


def N(d):
    return norm.cdf(d)


stockPrice = S = 31.55
timeUntilOptionExpiresInDays = t = 3.5 * 365.25
strikePrice = K = 22.75
riskFreeInterestRate = r = 5.0 / 100.0
sigma = volitivity_as_percentage_per_annum = 50.0
cumulativeStandatdNormalDistribution = N = lambda d : norm.cdf(d)
d1 = ( np.log(S/K) + (r + sigma**2/2.0)*t ) / (sigma * t**0.5)
d2 = d1 - sigma * t**0.5
C = S * N(d1) - K * np.exp(-r*t) * N(d2)
print d1
print d2
print C
print 10.0 / 365.0