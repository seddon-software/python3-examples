def fib():
    x = 0
    y = 1
    
    while(x < 100):
        x, y = x + y, x
        yield x  # return of the iterator
    return       # StopIteration of the iterator

g = fib()
g = g.__iter__()
print g.next()
print g.next()
print g.next()
